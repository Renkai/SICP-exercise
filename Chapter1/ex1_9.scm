;定义1
(define (+ 1 b)
  (if (= a 0)
      b
      (inc (+ (dec a) b)))
;定义2
(define (+ a b)
  (if (= a 0)
      b
      (+ (dec a) (inc b))))

;按照定义1 (+ 4 5)的过程为
(inc (inc (inc (inc (+ 0 5)))))
;是一个递归过程

;按照定义2 (+ 4 5)的过程为
(+ 3 6)
(+ 2 7)
(+ 1 8)
(+ 0 9)
;是一个迭代过程
